# Equivalent to the two commands above (evolve + retrain) plus generate_infographic.py,
# run back-to-back in one shot: evolve -> retrain -> infographic.
# -e experiment_path   -c evolve config_file   -d dataset   -n network_config
# -m fitness_metric (evolve)   -g config_code (retrain)   -R num_repetitions (retrain)
# -E max_epochs (retrain)
scripts/run_pipeline.sh \
    -e experiment_cifar10_mixedop/exp1 \
    -c configs/config_files_cifar/config_mixedop.yml \
    -d cifar10 \
    -n default \
    -m best_accuracy \
    -g run1_best \
    -R 5 \
    -E 300

